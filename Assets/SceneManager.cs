using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneManager : MonoBehaviour
{

    public IngredientManager ingredientManager;


    private string basePath = "Assets/Packages/Potion Making/Scriptable Objects/Base";

    // Read a JSON file that contains all of the items from a player inventory, 
    
    
    private void Start() {
        // Gets all the scriptable objects within the basePath folder and store it into a dictionary base on the itemID, afterwards 
    }

    private void ReadFile() {

    }
}
